# 测试模式与常见陷阱

测试模式
- 为 Atom/Computed 单元测试增量历史、事务回滚、装饰器行为；
- 为 react/reactor 测试 start/stop 与调度；
- 为 capture/parents 测试依赖修剪与脏检查。

常见陷阱
- 在反应中写依赖信号 → 循环；
- 过度依赖捕获 → 无关信号变更也触发；合理使用无捕获读；
- 历史长度过大导致内存与 pushEntry 成本高；
- 事务滥用/嵌套复杂，易出错；
- 装饰器 getter 误用：应使用方法装饰，而非 getter；
- 在高频路径频繁创建新信号实例。

